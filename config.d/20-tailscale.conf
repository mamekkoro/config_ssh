# Hosts resolved by Tailscale MagicDNS. With no HostName override, OpenSSH
# connects to the Host alias itself (for example, `ssh gin` resolves `gin`).
Host gin kurobox macomini minix moumou pizza pochi rmba shirobox
  User ryo
