# UseKeychain is an Apple OpenSSH extension. IgnoreUnknown must precede it so
# the same configuration remains valid on Linux OpenSSH.
Host *
  IgnoreUnknown UseKeychain
  AddKeysToAgent yes
  UseKeychain yes
